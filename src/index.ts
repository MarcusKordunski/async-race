import { listenCreateButton, listenRemoveButton, listenSelectUpdateButtons, listenViewButtons, render } from "./UI";
import './style.css'

render();
listenViewButtons();
listenRemoveButton()
listenCreateButton()
listenSelectUpdateButtons()